import { Link } from 'react-router-dom'
import type { Product } from '../../types'

export function ProductCard({ product }: { product: Product }) { const image = product.images[0]; const editionLabel = product.isLimitedEdition ? product.availabilityStatus === 'sold-out' ? 'Sold Out' : product.editionTotal !== undefined && product.editionSold !== undefined ? `${product.editionSold}/${product.editionTotal}` : product.editionBadge : undefined; return <Link className="product-card" to={`/product/${product.slug}`}><div className="product-image"><img src={image.src} alt={image.alt} width={image.width} height={image.height} loading="lazy" /></div><div className="product-meta"><div><h3>{product.name}</h3><p>${product.price.toFixed(2)}</p></div>{editionLabel && <span className="edition"><i />{editionLabel}</span>}</div></Link> }
